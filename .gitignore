# dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# testing
coverage/

# next.js
.next/
out/
build/
dist/

# production
*.tsbuildinfo
next-env.d.ts

# secrets — never commit these
.env
.env*.local
.env.production
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# deployment
.vercel

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# editors
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# internal working notes live in the private workspace, never here
BRIEF.md
SLOGAN.md
presupuesto*
*-privado.md

# The gallery photos ARE committed, as declared placeholders — the readme
# states plainly that they stand in for a photo session that has not
# happened and are not this (fictional) shop's work. Replace them with the
# real shoot and that declaration can go.
