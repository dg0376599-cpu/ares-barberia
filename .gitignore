# dependencies
node_modules/
.pnp
.pnp.js
.yarn/install-state.gz

# testing
coverage/

# next.js
.next/
out/
build/
dist/

# production
*.tsbuildinfo
next-env.d.ts

# secrets — never commit these
.env
.env*.local
.env.production
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# deployment
.vercel

# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# editors
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# internal working notes live in the private workspace, never here
BRIEF.md
SLOGAN.md
presupuesto*
*-privado.md

# Placeholder before/after photos — deliberately NOT committed.
# They arrived over WhatsApp at unusable resolution and, more to the
# point, they are not this barbershop's work: one pair has another
# shop's sign in the background. Publishing someone else's work as your
# own misleads customers, and the images are not ours to redistribute.
# Drop this rule once the real photo session replaces them.
/public/trabajos/
